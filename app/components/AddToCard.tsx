"use client";
import React from "react";

const AddToCard = () => {
  return (
    <button
      onClick={() => {
        console.log("clicked");
      }}
    >
      add
    </button>
  );
};

export default AddToCard;
